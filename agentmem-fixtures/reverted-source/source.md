# AgentMem GitHub Fixture: reverted source

This PR is merged, then another PR is created with a title matching GitHub's Revert convention. GitHubVCS should mark the source evidence as `reverted=True`.
