# AgentMem GitHub Fixture: revert marker

This fixture exists so the source PR has a real later PR titled `Revert "..."`. The file content is intentionally harmless; the test is about GitHub metadata.
